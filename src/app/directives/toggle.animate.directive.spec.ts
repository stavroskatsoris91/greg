import { ToggleAnimateDirective } from './toggle.animate.directive';

describe('ToggleAnimateDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleAnimateDirective();
    expect(directive).toBeTruthy();
  });
});
