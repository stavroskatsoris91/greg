import { ScrollToErrorDirective } from './scroll-to-error.directive';

describe('ScrollToErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollToErrorDirective();
    expect(directive).toBeTruthy();
  });
});
