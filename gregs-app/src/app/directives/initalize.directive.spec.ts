import { InitalizeDirective } from './initalize.directive';

describe('InitalizeDirective', () => {
  it('should create an instance', () => {
    const directive = new InitalizeDirective();
    expect(directive).toBeTruthy();
  });
});
